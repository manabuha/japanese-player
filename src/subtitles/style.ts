import {css} from 'lit';

const styles = css`
  :host {
    position: absolute;
    top: 12%;
    left: 0;
    right: 0;
    max-width: 80%;
    margin: 0 auto;
    text-align: center;
    cursor: default;
    box-sizing: border-box;
  }
`;

export default styles;
